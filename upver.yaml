version:
  file: zarf.yaml
  pattern: 'metadata:\s*\n(?:[ \t].*\n)*?[ \t]*version:\s*"?([^"\n]+)"?'
  group: 1

upstream:
  file: zarf.yaml
  pattern: 'registry\.jam-demo\.net/jam/ansys_license_manager/ansys_license_manager:([^\s"]+)'
  group: 1

changelog:
  file: CHANGELOG.md