version:
  file: zarf.yaml
  pattern: 'metadata:\s*\n(?:[ \t].*\n)*?[ \t]*version:\s*"?([^"\n]+)"?'
  group: 1

upstream:
  type: image-tag
  file: zarf.yaml
  imagePrefix: "registry.jam-demo.net/jam/ansys_license_manager/ansys_license_manager:"

changelog:
  file: CHANGELOG.md